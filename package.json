{
  "name": "dust-corridor",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Browser-native tactical FPS built on Three.js r180 with a fully custom WebGL2 render graph, physics, AI and procedural audio.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4319 --strictPort",
    "test": "playwright test",
    "test:install": "playwright install chromium",
    "profile": "playwright test tests/performance.spec.js --reporter=list"
  },
  "dependencies": {
    "three": "0.180.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "vite": "^7.0.0"
  }
}
